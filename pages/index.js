import React from 'react';

import Layout from '../components/layout/Layout';

const Home = () => {
    return (
        <Layout>
            <p>This is homepage.</p>
        </Layout>
    );
};

Home.propTypes = {};

export default Home;
