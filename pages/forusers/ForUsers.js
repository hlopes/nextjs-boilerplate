import React from 'react';

import Layout from '../../components/layout/Layout';

const ForUsers = () => {
    return (
        <Layout>
            <p>For Users only.</p>
        </Layout>
    );
};

export default ForUsers;
